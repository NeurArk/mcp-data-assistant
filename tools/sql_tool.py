"""
Placeholder for SQL query tool.

When implemented, function `run_sql(query: str) -> list[dict]` will
  * validate that the query is a read‑only SELECT,
  * execute it against a local SQLite database,
  * return the rows as a list of dictionaries.
"""
def run_sql(query: str):
    raise NotImplementedError("run_sql not yet implemented")
